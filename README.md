# Security Bot

Telegram бот для проверки безопасности криптоадресов, доменов и анализа Tron кошельков.

## Возможности

### 🔍 Проверка безопасности
- SafeCheck - общая проверка адресов на безопасность
- Rapira - детектирование вредоносных адресов и доменов
- Tron анализ - детальная информация о Tron кошельках

### 🔐 Система ролей
- **admin** - полный доступ к системе
- **moderator** - модерация контента
- **user** - базовые функции

### 📊 Функции администратора
- Управление пользователями и ролями
- Системная статистика и мониторинг
- Просмотр логов и отчетов безопасности
- Рассылка сообщений

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository_url>
cd telegram-bot-python
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id
RAPIRA_API_KEY=your_rapira_api_key
SAFECHECK_API_KEY=your_safecheck_api_key
```

4. Соберите проект:
```bash
npm run build
```

5. Запустите бота:
```bash
npm start
```

## Разработка

Запуск в режиме разработки:
```bash
npm run dev
```

Запуск с автоперезагрузкой:
```bash
npm run watch
```

## Команды пользователей

- `/start` - Запуск бота
- `/help` - Справка по командам
- `/check <адрес>` - Проверка адреса
- `/tron <адрес>` - Анализ Tron адреса
- `/rapira <адрес|домен>` - Проверка через Rapira
- `/profile` - Ваш профиль
- `/stats` - Статистика проверок
- `/support` - Поддержка

## Команды администратора

- `/admin` - Панель администратора
- `/users` - Список пользователей
- `/user <id>` - Информация о пользователе
- `/setrole <id> <role>` - Назначить роль
- `/systemstats` - Статистика системы
- `/logs [count]` - Просмотр логов
- `/broadcast <message>` - Рассылка
- `/clear_cache` - Очистить кеши
- `/maintenance` - Режим обслуживания

## Структура проекта

```
src/
├── bot.ts                 # Главный файл бота
├── types/                 # TypeScript типы
├── models/                # Модели данных
├── services/              # Бизнес-логика
│   ├── userService.ts     # Управление пользователями
│   ├── logService.ts      # Логирование
│   ├── actionService.ts   # Обработка запросов
│   ├── safeCheckService.ts # Проверка безопасности
│   ├── tronService.ts     # Tron анализ
│   └── rapiraService.ts   # Rapira интеграция
├── commands/              # Команды бота
│   ├── userCommands.ts    # Пользовательские команды
│   └── adminCommands.ts   # Административные команды
├── middleware/            # Middleware
│   └── authMiddleware.ts  # Авторизация и RBAC
└── utils/                 # Утилиты
```

## API интеграции

### SafeCheck API
Для проверки безопасности адресов. Настройте `SAFECHECK_API_KEY` в .env файле.

### Rapira API
Для детектирования вредоносных адресов и доменов. Настройте `RAPIRA_API_KEY` в .env файле.

### Tron API
Использует публичные API TronGrid и TronScan для анализа Tron адресов.

## Безопасность

- Система ролей и разрешений (RBAC)
- Логирование всех действий
- Ограничение частоты запросов
- Кеширование результатов проверок
- Режим обслуживания

## Мониторинг

Бот ведет подробные логи всех операций:
- Действия пользователей
- Результаты проверок
- Ошибки системы
- Попытки несанкционированного доступа

## Поддержка

Для получения помощи используйте команду `/support` в боте или обратитесь к администратору.

## Лицензия

ISC